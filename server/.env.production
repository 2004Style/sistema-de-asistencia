# ============================================
# ARCHIVO .env PARA PRODUCCIÓN - BD EXTERNA
# ============================================
# 
# INSTRUCCIONES:
# 1. Copia este archivo como .env en la carpeta server/
# 2. Reemplaza los valores de ejemplo con los tuyos
# 3. IMPORTANTE: Cambia los valores sensibles (passwords, secret_key)
# 4. NO commits este archivo a GitHub (está en .gitignore)
#
# UBICACIÓN: /home/deploy/app/server/.env

# ============================================
# BASE DE DATOS EXTERNA (En OTRO servidor)
# ============================================

# URL completa de conexión PostgreSQL
# Formato: postgresql://usuario:contraseña@host:puerto/base_datos
# Ejemplo: postgresql://rdev:rdev@192.168.1.100:5432/asistencia
DATABASE_URL=postgresql://rdev:rdev@tu-ip-servidor-bd:5432/asistencia

# ============================================
# API - FASTAPI
# ============================================

# Host en el que escucha (0.0.0.0 para aceptar conexiones externas)
HOST=0.0.0.0

# Puerto en el que corre la API internamente (8000)
# NOTA: Nginx expone puerto 80, pero la API corre en 8000
PORT=8000

# Timezone
TIMEZONE=America/Lima

# Debug mode (NUNCA activar en producción)
DEBUG=false

# ============================================
# NGINX - PUERTOS PÚBLICOS
# ============================================

# Puerto HTTP (puerto 80 estándar)
NGINX_PORT=80

# Puerto HTTPS (puerto 443 estándar, solo si tienes SSL)
NGINX_SSL_PORT=443

# ============================================
# SEGURIDAD - JWT TOKENS
# ============================================

# CLAVE SECRETA para firmar JWT (CAMBIAR ESTO)
# Genera una clave segura: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=tu-clave-secreta-super-larga-y-complicada-aqui-cambiala-en-produccion-12345

# ============================================
# CORREO ELECTRÓNICO - MAIL API
# ============================================

# URL base del servicio de API de correos
MAIL_API_URL=https://api.mail-service.com

# Email y credenciales del remitente
MAIL_API_CLIENT_ID=tu-client-id
MAIL_API_SECRET=tu-api-secret

# Email de origen
SMTP_FROM_EMAIL=tu-email@yourdomain.com
SMTP_FROM_NAME=Sistema de Asistencia

# ============================================
# ALERTAS Y UMBRALES
# ============================================

# Máximo de tardanzas antes de alerta
TARDANZAS_MAX_ALERTA=3

# Máximo de faltas antes de alerta
FALTAS_MAX_ALERTA=2

# Minutos de retraso para contar como tardanza
MINUTOS_TARDANZA=15
