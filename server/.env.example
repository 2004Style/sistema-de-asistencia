# ============================================
# server/.env.example - REFERENCIA
# ============================================
# ⚠️ NOTA IMPORTANTE:
#
# En producción con Docker, NO necesitas este archivo
# El servidor lee todas las variables desde .env (raíz)
# configurado en docker-compose.yml:
#
#   api:
#     env_file:
#       - .env    ← Lee de aquí
#
# Este archivo es SOLO una referencia de qué variables
# usa el servidor FastAPI.
#
# Para desarrollo LOCAL (sin Docker), copia este archivo
# a server/.env y personaliza los valores.
#
# ============================================

# ============================================
# LECTURA DESDE .env RAÍZ (en Docker)
# ============================================
# Las siguientes variables las hereda desde .env raíz:
#
# - DATABASE_URL
# - SECRET_KEY
# - JWT_SECRET_KEY
# - HOST
# - PORT
# - TIMEZONE
# - CORS_ORIGINS
# - AUTO_MIGRATE
# - DEBUG
# - ENVIRONMENT
# - NODE_ENV
# - MAIL_API_URL
# - MAIL_API_CLIENT_ID
# - MAIL_API_SECRET
# - SMTP_FROM_EMAIL
# - SMTP_FROM_NAME
# - TARDANZAS_MAX_ALERTA
# - FALTAS_MAX_ALERTA
# - MINUTOS_TARDANZA
# - MAX_FILE_SIZE
# - UPLOAD_DIR
# - REPORTS_DIR
# - TEMP_DIR
# - PASSWORD_MIN_LENGTH
#
# ============================================

# Si usas desarrollo LOCAL (sin Docker), configura aquí:

# BASE DE DATOS
DATABASE_URL=postgresql://rdev:rdev@localhost:5432/asistencia

# SEGURIDAD
SECRET_KEY=your-secret-key-change-in-production-REPLACE-ME
JWT_SECRET_KEY=your-jwt-secret-key-change-in-production-REPLACE-ME

# API
HOST=0.0.0.0
PORT=8000
TIMEZONE=America/Lima

# CORS (para desarrollo sin Docker)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:8000

# AMBIENTE
AUTO_MIGRATE=true
DEBUG=true
ENVIRONMENT=development

# ARCHIVOS
MAX_FILE_SIZE=10485760
UPLOAD_DIR=recognize/data
REPORTS_DIR=public/reports
TEMP_DIR=public/temp
PASSWORD_MIN_LENGTH=8

# ALERTAS
TARDANZAS_MAX_ALERTA=3
FALTAS_MAX_ALERTA=2
MINUTOS_TARDANZA=15
